# In Progress

这份文档用于维护当前正在开发中的功能列表、当前阶段概览和计划入口。具体步骤计划统一放在 `docs/plan/` 目录下。功能稳定完成后，需要将对应记录迁移到 `docs/system-state.md`。

## 正在做

- 更低延迟的流式 ASR：当前聚焦真实 partial transcript、增量去重和最终稿收敛能力；步骤计划见 `docs/plan/` 下对应计划文件。
- 更强的 turn taking：当前聚焦短停顿、补充发言、抢话、假打断和 backchannel 处理；步骤计划见 `docs/plan/` 下对应计划文件。
- 更好的流式 TTS 编排：当前聚焦句级/短语级调度、首包时延和播放连贯性；步骤计划见 `docs/plan/` 下对应计划文件。
- 更标准的实时传输层验证：当前聚焦 WebRTC 弱网恢复、高并发稳定性和多浏览器兼容验证；步骤计划见 `docs/plan/` 下对应计划文件。

## 维护约定

- 这里先记录“当前在做什么”和“已经推进到哪一类阶段”。
- 需要落具体执行步骤、依赖顺序、验收点和阶段进展时，在 `docs/plan/` 下新增独立计划文件。
- 一个功能的详细步骤只放在一个计划文件里，避免 `in-progress.md` 和计划文件重复维护。
